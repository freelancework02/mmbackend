<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <% 
    /* =========================
       SAFE SEO INPUTS & FALLBACKS
       ========================= */
    const SITE_NAME   = "Maula Ali Research Center";
    const _baseHref   = (typeof baseHref === "string" && baseHref.trim()) ? baseHref.trim() : "https://demo.minaramasjid.com/";
    const BASE        = _baseHref.replace(/\/+$/,"");            // no trailing slash
    const q           = (question && typeof question === "object") ? question : {};
    const qId         = q.id || q._id || q.questionId || "";
    const qTitleUr    = q.titleUrdu || q.questionUrdu || "";
    const qTitleEn    = q.titleEnglish || q.questionEnglish || "";
    const qAnsUr      = q.answerUrdu || "";
    const qAnsEn      = q.answerEnglish || "";
    const toText      = (s)=> String(s||"").replace(/<[^>]*>/g,"").replace(/\s+/g," ").trim();

    // Canonical: force id-only route
    const canonicalUrl= `${BASE}/question/${qId}`;

    const computedTitle = qTitleUr || qTitleEn || "Question Detail";
    const metaT         = (typeof metaTitle === "string" && metaTitle.trim())
                          ? metaTitle
                          : `${computedTitle} | ${SITE_NAME}`;

    const descCandidate = toText(qAnsEn) || toText(qAnsUr) || toText(qTitleEn) || toText(qTitleUr) || "Read this detailed fatwa.";
    const metaD         = (typeof metaDesc === "string" && metaDesc.trim())
                          ? metaDesc
                          : descCandidate.slice(0, 170);

    // You can plug in your own image per-question if you have it, else fallback
    const fallbackOg    = "https://placehold.co/1200x630/6a8a4f/ffffff?text=Fatwa+%7C+Question+%26+Answer";
    const metaI         = (typeof metaImage === "string" && metaImage.trim()) ? metaImage : fallbackOg;

    // Dates (optional)
    const datePublished = q.dateG || q.createdOn || null;
    const dateModified  = q.modifiedOn || q.dateG || q.createdOn || null;

    // Breadcrumbs
    const breadcrumbs = [
      { name: "Home",    url: `${BASE}/` },
      { name: "Q&A",     url: `${BASE}/qa` },
      { name: toText(computedTitle) || "Question", url: canonicalUrl }
    ];

    const askerName   = toText(q.asker || "نامعلوم");
    const muftiName   = toText(q.mufti || "");
    const madrasaName = toText(q.madrasa || "");
    const cityName    = toText(q.city || "");
  %>

  <!-- ===== Primary SEO ===== -->
  <title><%= metaT %></title>
  <meta name="description" content="<%= metaD %>">
  <meta name="keywords" content="Maula Ali Research Centre, Islamic Questions, Fatwa, Islamic Rulings, Ask Scholars, Urdu Fatwa, Q&A, Darulifta">
  <meta name="robots" content="index,follow,max-image-preview:large">
  <link rel="canonical" href="<%= canonicalUrl %>">

  <!-- ===== Open Graph ===== -->
  <meta property="og:site_name" content="Maula Ali Research Center">
  <meta property="og:title" content="<%= metaT %>">
  <meta property="og:description" content="<%= metaD %>">
  <meta property="og:image" content="<%= metaI %>">
  <meta property="og:image:alt" content="<%= toText(computedTitle) %>">
  <meta property="og:type" content="article">
  <meta property="og:url" content="<%= canonicalUrl %>">
  <% if (datePublished) { %><meta property="article:published_time" content="<%= new Date(datePublished).toISOString() %>"><% } %>
  <% if (dateModified)  { %><meta property="article:modified_time"  content="<%= new Date(dateModified).toISOString() %>"><% } %>
  <meta property="article:section" content="Fatwa">

  <!-- ===== Twitter Cards ===== -->
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:title" content="<%= metaT %>">
  <meta name="twitter:description" content="<%= metaD %>">
  <meta name="twitter:image" content="<%= metaI %>">

  <!-- ===== Hreflang (same canonical if single page) ===== -->
  <link rel="alternate" href="<%= canonicalUrl %>" hreflang="en">
  <link rel="alternate" href="<%= canonicalUrl %>" hreflang="ur">
  <link rel="alternate" href="<%= canonicalUrl %>" hreflang="x-default">

  <!-- ===== JSON-LD: WebSite, Organization, BreadcrumbList, QAPage ===== -->
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@graph": [
      {
        "@type": "WebSite",
        "@id": "<%= BASE %>#website",
        "url": "<%= BASE %>/",
        "name": "Maula Ali Research Center",
        "inLanguage": "ur",
        "potentialAction": {
          "@type": "SearchAction",
          "target": "<%= BASE %>/search?q={search_term_string}",
          "query-input": "required name=search_term_string"
        }
      },
      {
        "@type": "Organization",
        "@id": "<%= BASE %>#organization",
        "name": "Maula Ali Research Center",
        "url": "<%= BASE %>/",
        "logo": {
          "@type": "ImageObject",
          "url": "https://minaramasjid.com/assets/image/logo/marc.png"
        },
        "sameAs": []
      },
      {
        "@type": "BreadcrumbList",
        "@id": "<%= canonicalUrl %>#breadcrumbs",
        "itemListElement": [
          <% breadcrumbs.forEach(function(bc, i){ %>
          { "@type": "ListItem", "position": <%= i+1 %>, "name": "<%= bc.name %>", "item": "<%= bc.url %>" }<%= (i<breadcrumbs.length-1) ? "," : "" %>
          <% }) %>
        ]
      },
      {
        "@type": "QAPage",
        "@id": "<%= canonicalUrl %>#qapage",
        "url": "<%= canonicalUrl %>",
        "name": "<%= toText(computedTitle).replace(/\"/g,'\\\"') %>",
        "inLanguage": "ur",
        "mainEntity": {
          "@type": "Question",
          "name": "<%= toText(computedTitle).replace(/\"/g,'\\\"') %>",
          "text": "<%= toText(q.questionUrdu || q.questionEnglish || computedTitle).replace(/\"/g,'\\\"') %>",
          <% if (datePublished) { %>"dateCreated": "<%= new Date(datePublished).toISOString() %>",<% } %>
          <% if (askerName) { %>"author": { "@type": "Person", "name": "<%= askerName.replace(/\"/g,'\\\"') %>" },<% } %>
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "<%= toText(qAnsUr || qAnsEn).replace(/\"/g,'\\\"') %>",
            <% if (dateModified) { %>"dateCreated": "<%= new Date(dateModified).toISOString() %>",<% } %>
            "author": {
              "@type": "Person",
              "name": "<%= (muftiName || 'Mufti').replace(/\"/g,'\\\"') %>"
            }
          }
        },
        "publisher": { "@id": "<%= BASE %>#organization" },
        "image": { "@type": "ImageObject", "url": "<%= metaI %>" }
      }
    ]
  }
  </script>

  <!-- Base Href -->
  <base href="<%= BASE %>/" />

  <link rel="icon" href="https://minaramasjid.com/assets/image/logo/favicon.ico" type="image/x-icon" />

  <!-- Tailwind -->
  <script src="https://cdn.tailwindcss.com"></script>

  <!-- Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Roboto+Flex:opsz,wght@8..144,400;8..144,500;8..144,700&display=swap" rel="stylesheet" />
  <link href="https://fonts.googleapis.com/css2?family=Gulzar&display=swap" rel="stylesheet" />

  <!-- Icons -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css" />

  <style>
    @font-face {
      font-family: 'NaatFont';
      src: url('https://res.cloudinary.com/awescreative/raw/upload/v1749150996/Awes/naatfont.ttf') format('truetype');
      font-weight: normal; font-style: normal; font-display: swap;
    }
    body { font-family: "Roboto Flex", sans-serif; }
    .rtl { direction: rtl; }
    .rounded-4xl { border-radius: 2rem; }
    .mobile-menu-hidden { max-height: 0; opacity: 0; transition: max-height .3s ease-out, opacity .2s ease-out; overflow: hidden; }
    .mobile-menu-visible { max-height: 500px; opacity: 1; transition: max-height .4s ease-in, opacity .3s ease-in; }
    .gulzartext, .heading, .prose-rtl { font-family: 'NaatFont','Gulzar',serif; }
    .prose { color:#333; }
    .prose p { margin-bottom: 1em; }
    .prose-rtl { direction: rtl; text-align: right; }
    .prose-ltr { direction: ltr; text-align: left; }
    @keyframes fadeInUp { from { opacity:0; transform: translateY(20px); } to { opacity:1; transform: translateY(0);} }
    .animate-fade-in-up { animation: fadeInUp .6s ease-out forwards; opacity:0; }
  </style>
</head>

<body class="font-sans bg-[#f0f5e9] text-gray-800 antialiased">
  <!-- Background -->
  <div class="absolute inset-0 bg-cover bg-no-repeat opacity-70" style="background-image:url('/assets/bg-DgzunbF9.png'); background-position:center; z-index:-1;"></div>

  <!-- Header -->
  <header class="bg-[#718e56] sticky top-0 z-50 shadow-md border-b border-green-100">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between items-center py-3 relative">
        <nav class="hidden md:flex gap-5 text-md font-semibold text-white tracking-wide">
          <a href="/" class="hover:text-green-100 transition-colors">Home</a>
          <a href="/about" class="hover:text-green-100 transition-colors">About</a>
          <a href="/events" class="hover:text-green-100 transition-colors">News & Event</a>
          <a href="/book" class="hover:text-green-100 transition-colors">Books</a>
        </nav>
        <div class="absolute left-1/2 transform -translate-x-1/2 -bottom-7 bg-white rounded-full p-1 shadow-lg border border-green-100 z-10">
          <a href="/"><img src="https://minaramasjid.com/assets/image/logo/marc.png" alt="Logo" class="w-16 h-16 rounded-full object-contain" /></a>
        </div>
        <nav class="hidden md:flex gap-5 text-md font-semibold text-white tracking-wide">
          <a href="/article" class="hover:text-green-100 transition-colors">Articles</a>
          <a href="/qa" class="text-white font-bold" aria-current="page">Question Answer</a>
          <a href="/book-request" class="hover:text-green-100 transition-colors">Request a Book</a>
          <a href="/contact" class="hover:text-green-100 transition-colors">Contact</a>
        </nav>
        <div class="md:hidden flex-1 flex justify-end">
          <button id="mobile-menu-toggle" class="text-white focus:outline-none" aria-label="Open menu">
            <i class="fas fa-bars w-7 h-7"></i>
          </button>
        </div>
      </div>
      <div id="mobile-menu" class="md:hidden mobile-menu-hidden">
        <div class="flex flex-col gap-2 py-3 px-2 text-white bg-[#5a7544] rounded-b-xl">
          <a href="/" class="hover:bg-[#4f6639] px-4 py-2 rounded">Home</a>
          <a href="/about" class="hover:bg-[#4f6639] px-4 py-2 rounded">About</a>
          <a href="/events" class="hover:bg-[#4f6639] px-4 py-2 rounded">News & Event</a>
          <a href="/book" class="hover:bg-[#4f6639] px-4 py-2 rounded">Books</a>
          <a href="/articles" class="hover:bg-[#4f6639] px-4 py-2 rounded">Articles</a>
          <a href="/qa" class="bg-[#4f6639] px-4 py-2 rounded" aria-current="page">Question Answer</a>
          <a href="/book-request" class="hover:bg-[#4f6639] px-4 py-2 rounded">Request a Book</a>
          <a href="/contact" class="hover:bg-[#4f6639] px-4 py-2 rounded">Contact</a>
        </div>
      </div>
    </div>
  </header>

  <!-- Main -->
  <main class="relative z-10 pt-12 sm:pt-16">
    <section class="relative max-w-7xl mx-auto px-4 py-8">
      <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
        <!-- Main Column -->
        <div class="lg:col-span-2 lg:order-2">
          <div class="bg-white p-6 md:p-8 rounded-3xl mb-8 shadow-xl animate-fade-in-up">
            <div class="rtl text-right">
              <h1 class="heading text-2xl lg:text-4xl font-bold text-[#4a7031] mb-3" lang="ur">
                <%= stripTags(
                  q.titleUrdu || q.questionUrdu || q.titleEnglish || q.questionEnglish || metaTitle || ''
                ) %>
              </h1>

              <div class="mb-4 pb-4 border-b border-gray-200">
                <p class="gulzartext text-md text-gray-700">
                  پوچھنے والا: <%= stripTags(q.asker || 'نامعلوم') %>
                </p>
                <p class="gulzartext text-sm text-gray-500 mt-1">
                  <% if (q.madrasa) { %>دارالافتاء: <%= stripTags(q.madrasa) %><% } %>
                  <% if (q.mufti) { %><span class="mx-2">|</span> کتبہ: <%= stripTags(q.mufti) %><% } %>
                  <% if (q.city) { %><span class="mx-2">|</span> <%= stripTags(q.city) %><% } %>
                </p>
              </div>

              <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-3 mb-6">
                <div class="flex items-center gap-2 text-gray-500">
                  <i class="fas fa-eye w-4 text-gray-400" aria-hidden="true"></i>
                  <span class="text-md font-bold text-gray-900"><%= Number(q.views || 0) %></span>
                  <span class="text-sm font-semibold">Views</span>
                </div>

                <a id="shareBtn"
                   title="Share question"
                   class="inline-flex items-center justify-center gap-2 bg-[#e8f0e0] text-[#4a7031] rounded-full hover:bg-[#d6e5c4] transition duration-300 shadow-sm px-4 py-2 text-sm font-semibold cursor-pointer">
                  <i class="fas fa-share-alt" aria-hidden="true"></i>
                  <span>Share</span>
                </a>
              </div>
            </div>

            <!-- Language Switcher -->
            <div class="flex rounded-full overflow-hidden border border-[#d6e5c4] mb-6">
              <% 
                const hasUr = !!(q.questionUrdu || q.titleUrdu || questionHTML);
                const hasEn = !!(q.questionEnglish || q.titleEnglish || q.answerEnglish);
                const hasRo = !!(q.romanUrdu || q.questionRoman || q.answerRoman);
                const initialLang = hasUr ? 'ur' : (hasEn ? 'en' : (hasRo ? 'ro' : 'ur'));
              %>
              <button id="lang-ur" class="lang-btn gulzartext flex-1 text-center py-2 text-sm cursor-pointer <%= initialLang==='ur' ? 'bg-[#c1d9a3] text-[#4a7031] font-semibold' : 'bg-[#e8f0e0] hover:bg-[#d6e5c4] text-gray-700 font-medium' %>">اردو</button>
              <button id="lang-ro" class="lang-btn flex-1 text-center py-2 text-sm cursor-pointer <%= initialLang==='ro' ? 'bg-[#c1d9a3] text-[#4a7031] font-semibold' : 'bg-[#e8f0e0] hover:bg-[#d6e5c4] text-gray-700 font-medium' %>">Roman</button>
              <button id="lang-en" class="lang-btn flex-1 text-center py-2 text-sm cursor-pointer <%= initialLang==='en' ? 'bg-[#c1d9a3] text-[#4a7031] font-semibold' : 'bg-[#e8f0e0] hover:bg-[#d6e5c4] text-gray-700 font-medium' %>">English</button>
            </div>

            <!-- Urdu -->
            <div id="content-ur" class="lang-content prose prose-rtl max-w-none gulzartext text-lg leading-relaxed <%= initialLang==='ur' ? '' : 'hidden' %>">
              <h2 class="heading text-2xl font-bold text-[#4a7031] mb-3">سوال:</h2>
              <div id="fatwa-question" dir="<%= detectDirection(stripTags(q.questionUrdu || q.questionEnglish || '')) %>">
                <% if (questionHTML) { %>
                  <%- questionHTML %>
                <% } else { %>
                  <p><%= stripTags(q.questionUrdu || q.questionEnglish || '') %></p>
                <% } %>
              </div>

              <hr class="my-6">

              <h2 class="heading text-2xl font-bold text-[#4a7031] mb-3">جواب:</h2>
              <div id="fatwa-answer" dir="<%= detectDirection(stripTags(q.answerUrdu || q.answerEnglish || '')) %>">
                <% if (answerHTML) { %>
                  <%- answerHTML %>
                <% } else { %>
                  <p><%= stripTags(q.answerUrdu || q.answerEnglish || '') %></p>
                <% } %>

                <% if (q.mufti || q.madrasa || q.city || q.dateHijri || q.dateG || q.sealImage){ %>
                  <div class="mt-8 pt-5 border-t border-dashed border-green-200 rtl text-right text-[15px] text-[#2b2b2b]">
                    <% if (q.mufti) { %><div><strong>المفتی:</strong> <%= stripTags(q.mufti) %></div><% } %>
                    <% if (q.madrasa) { %><div><strong>دارالافتاء:</strong> <%= stripTags(q.madrasa) %></div><% } %>
                    <% if (q.city) { %><div><strong>شہر:</strong> <%= stripTags(q.city) %></div><% } %>
                    <% if (q.dateHijri || q.dateG) { %>
                      <div>
                        <% if (q.dateHijri) { %><strong>تاریخ (ہجری):</strong> <%= stripTags(q.dateHijri) %><% } %>
                        <% if (q.dateG) { %><span class="mx-2">/</span><strong>تاریخ (عیسوی):</strong> <%= stripTags(q.dateG) %><% } %>
                      </div>
                    <% } %>
                    <% if (q.sealImage) { %>
                      <div class="mt-2"><img src="<%= q.sealImage %>" alt="مہر" class="inline-block max-h-24 object-contain"></div>
                    <% } %>
                  </div>
                <% } %>
              </div>
            </div>

            <!-- Roman Urdu -->
            <div id="content-ro" class="lang-content prose prose-ltr max-w-none text-md leading-relaxed <%= initialLang==='ro' ? '' : 'hidden' %>">
              <h2 class="text-2xl font-bold text-[#4a7031] mb-3">Sawaal:</h2>
              <p><%= stripTags(q.questionRoman || q.romanUrdu || '') %></p>
              <hr class="my-6">
              <h2 class="text-2xl font-bold text-[#4a7031] mb-3">Jawaab:</h2>
              <p><%= stripTags(q.answerRoman || '') %></p>
            </div>

            <!-- English -->
            <div id="content-en" class="lang-content prose prose-ltr max-w-none text-md leading-relaxed <%= initialLang==='en' ? '' : 'hidden' %>">
              <h2 class="text-2xl font-bold text-[#4a7031] mb-3">Question:</h2>
              <p><%= stripTags(q.questionEnglish || '') %></p>
              <hr class="my-6">
              <h2 class="text-2xl font-bold text-[#4a7031] mb-3">Answer:</h2>
              <p><%= stripTags(q.answerEnglish || '') %></p>
            </div>
          </div>

          <!-- Related (mobile) -->
          <div class="animate-fade-in-up lg:hidden" style="animation-delay:300ms;">
            <div class="flex flex-col sm:flex-row justify-between items-center mb-6 gap-4 rtl text-right">
              <h2 class="heading text-2xl font-bold text-[#4a7031]">تازہ ترین فتاویٰ</h2>
              <a href="/all-faq" class="bg-white border border-[#4a7031] text-[#4a7031] rounded-full px-4 py-2 text-sm font-medium hover:bg-[#eaf3df] transition-colors duration-300 shadow-sm flex-shrink-0 gulzartext">سب دیکھیں</a>
            </div>

            <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
              <% (Array.isArray(related) ? related : []).slice(0,6).forEach(function(r, idx){ %>
                <div class="bg-white rounded-2xl shadow-lg overflow-hidden transition-all duration-300 hover:shadow-2xl hover:-translate-y-1 h-full">
                  <a href="/question/<%= r.id %>/<%= slugify(r.slug || (r.questionEnglish || r.questionUrdu || ('question-' + (r.id || idx+1)))) %>"
                     class="group flex flex-col h-full p-4">
                    <div class="flex-grow">
                      <h3 class="heading text-gray-800 group-hover:text-[#4a7031] text-lg font-bold mb-2 truncate rtl text-right transition-colors duration-200">
                        <%= stripTags(r.titleUrdu || r.questionUrdu || r.titleEnglish || r.questionEnglish || '') %>
                      </h3>
                      <p class="gulzartext text-gray-600 text-sm line-clamp-1 rtl text-right">
                        <%= stripTags(r.excerptUrdu || r.questionUrdu || r.excerptEnglish || r.questionEnglish || '') %>
                      </p>
                    </div>
                    <div class="mt-4 pt-3 border-t border-gray-100 flex justify-between items-center">
                      <div class="flex items-center gap-1 text-xs text-gray-500">
                        <i class="fas fa-eye fa-xs" aria-hidden="true"></i>
                        <span><%= Number(r.views || 0) %></span>
                      </div>
                      <span class="gulzartext text-xs font-semibold text-[#4a7031] inline-flex items-center rtl">
                        مزید پڑھیں
                        <i class="fa-solid fa-arrow-left text-xs ml-2 transition-transform group-hover:-translate-x-1" aria-hidden="true"></i>
                      </span>
                    </div>
                  </a>
                </div>
              <% }); %>
            </div>
          </div>
        </div>

        <!-- Sidebar -->
        <aside class="hidden lg:block lg:col-span-1 space-y-6 animate-fade-in-up lg:order-1" style="animation-delay:150ms;">
          <div class="sticky top-24">
            <h3 class="heading text-2xl font-bold text-[#4a7031] mb-4 rtl text-right">متعلقہ سوالات</h3>

            <% (Array.isArray(related) ? related : []).slice(0,10).forEach(function(r, idx){ %>
              <a href="/question/<%= r.id %>/<%= slugify(r.slug || (r.questionEnglish || r.questionUrdu || ('question-' + (r.id || idx+1)))) %>"
                 class="group flex items-start gap-3 bg-white p-4 rounded-2xl shadow-lg hover:shadow-xl hover:-translate-y-0.5 transition-all duration-300 mb-4 border border-transparent hover:border-[#c1d9a3]">
                <div class="flex-shrink-0 w-10 h-10 rounded-full bg-[#4a7031] flex items-center justify-center">
                  <span class="font-bold text-lg text-white"><%= (idx+1) %></span>
                </div>
                <div class="flex-1 rtl text-right">
                  <h4 class="heading text-gray-800 group-hover:text-[#4a7031] text-md font-semibold leading-snug line-clamp-1 transition-colors duration-200">
                    <%= stripTags(r.titleUrdu || r.questionUrdu || r.titleEnglish || r.questionEnglish || '') %>
                  </h4>
                  <div class="flex items-center gap-1.5 text-xs text-gray-500 mt-1.5">
                    <i class="fas fa-eye fa-xs" aria-hidden="true"></i>
                    <span><%= Number(r.views || 0) %></span>
                  </div>
                </div>
              </a>
            <% }); %>

            <a href="/all-faq" class="block text-center w-full bg-white border border-[#4a7031] text-[#4a7031] rounded-full px-4 py-2 text-sm font-medium hover:bg-[#eaf3df] transition-colors duration-300 shadow-sm gulzartext">سب دیکھیں</a>
          </div>
        </aside>
      </div>
    </section>
  </main>

  <!-- Toast -->
  <div id="toast" class="fixed bottom-24 right-6 bg-gray-800 text-white px-5 py-3 rounded-lg shadow-lg opacity-0 transition-opacity duration-300 z-50">
    Link copied to clipboard!
  </div>

  <!-- Footer -->
  <footer class="bg-[#718e56] text-white mt-16 sm:mt-20 relative z-20">
    <div class="max-w-7xl mx-auto px-6 lg:px-8 py-8">
      <div class="flex flex-col items-center">
        <a href="/index" class="flex items-center space-x-3 mb-5">
          <img class="h-12 w-auto bg-white rounded-full p-1" src="https://minaramasjid.com/assets/image/logo/marc.png" alt="Maula Ali Research Center Logo" />
          <span class="text-lg font-bold text-white">Maula Ali Research Center</span>
        </a>
        <div class="flex space-x-5">
          <a href="#" class="text-green-200 hover:text-white transition-colors" aria-label="Facebook"><i class="fab fa-facebook-f fa-lg"></i></a>
          <a href="#" class="text-green-200 hover:text-white transition-colors" aria-label="Twitter"><i class="fab fa-twitter fa-lg"></i></a>
          <a href="#" class="text-green-200 hover:text-white transition-colors" aria-label="Instagram"><i class="fab fa-instagram fa-lg"></i></a>
          <a href="#" class="text-green-200 hover:text-white transition-colors" aria-label="LinkedIn"><i class="fab fa-linkedin-in fa-lg"></i></a>
        </div>
      </div>
      <div class="mt-8 pt-5 border-t border-green-400/50 text-center">
        <p class="text-sm text-green-200">
          &copy; 2024 - 2025 Design and Develop by
          <a href="https://www.AlQalamArts.com/" target="_blank" rel="noopener noreferrer" class="font-medium text-white hover:text-green-100">www.AlQalamArts.com</a>
        </p>
      </div>
    </div>
  </footer>

  <!-- Back To Top -->
  <button id="backToTopBtn" class="fixed bottom-6 right-6 bg-[#4a7031] hover:bg-[#3b5a28] text-white w-10 h-10 rounded-full shadow-lg hidden justify-center items-center transition-all duration-300 opacity-0 z-40" aria-label="Back to top">
    <i class="fas fa-arrow-up"></i>
  </button>

  <!-- Mobile Bottom Navigation Bar -->
  <div id="mobile-navbar" class="fixed bottom-0 left-0 right-0 bg-white shadow-xl z-40 lg:hidden rounded-t-2xl border-t border-gray-200">
    <nav class="flex justify-around items-center h-16 text-gray-400">
      <a href="/" class="mobile-nav-item flex flex-col items-center justify-center p-2 text-sm hover:text-[var(--primary-dark)] transition-colors duration-200">
        <i class="fas fa-home text-lg mobile-nav-icon"></i><span class="mt-1 mobile-nav-text">Home</span>
      </a>
      <a href="/qa" class="mobile-nav-item flex flex-col items-center justify-center p-2 text-sm hover:text-[var(--primary-dark)] transition-colors duration-200">
        <i class="fas fa-question-circle text-lg mobile-nav-icon"></i><span class="mt-1 mobile-nav-text">Q&A</span>
      </a>
      <a href="/book" class="mobile-nav-item flex flex-col items-center justify-center p-2 text-sm hover:text-[var(--primary-dark)] transition-colors duration-200">
        <i class="fas fa-book text-lg mobile-nav-icon"></i><span class="mt-1 mobile-nav-text">Books</span>
      </a>
      <a href="/article" class="mobile-nav-item flex flex-col items-center justify-center p-2 text-sm hover:text-[var(--primary-dark)] transition-colors duration-200">
        <i class="fas fa-newspaper text-lg mobile-nav-icon"></i><span class="mt-1 mobile-nav-text">Articles</span>
      </a>
      <a href="/gallery" class="mobile-nav-item flex flex-col items-center justify-center p-2 text-sm hover:text-[var(--primary-dark)] transition-colors duration-200">
        <i class="fas fa-images text-lg mobile-nav-icon"></i><span class="mt-1 mobile-nav-text">Gallery</span>
      </a>
    </nav>
  </div>

  <!-- JS -->
  <script type="module">
    document.addEventListener('DOMContentLoaded', () => {
      // Mobile menu
      const mobileMenuButton = document.getElementById('mobile-menu-toggle');
      const mobileMenu = document.getElementById('mobile-menu');
      if (mobileMenuButton && mobileMenu) {
        mobileMenuButton.addEventListener('click', () => {
          mobileMenu.classList.toggle('mobile-menu-hidden');
          mobileMenu.classList.toggle('mobile-menu-visible');
        });
      }

      // Back to top
      const backToTopBtn = document.getElementById('backToTopBtn');
      if (backToTopBtn) {
        window.addEventListener('scroll', () => {
          if (window.scrollY > 300) {
            backToTopBtn.classList.remove('hidden','opacity-0');
            backToTopBtn.classList.add('flex');
          } else {
            backToTopBtn.classList.add('opacity-0');
            setTimeout(() => {
              if (window.scrollY <= 300) {
                backToTopBtn.classList.add('hidden');
                backToTopBtn.classList.remove('flex');
              }
            }, 300);
          }
        });
        backToTopBtn.addEventListener('click', () => window.scrollTo({ top:0, behavior:'smooth' }));
      }

      // Language tabs
      const langButtons = document.querySelectorAll('.lang-btn');
      const langContents = document.querySelectorAll('.lang-content');
      if (langButtons.length && langContents.length) {
        langButtons.forEach(button => {
          button.addEventListener('click', () => {
            const lang = button.id.split('-')[1];
            langButtons.forEach(btn => {
              btn.classList.remove('bg-[#c1d9a3]','text-[#4a7031]','font-semibold');
              btn.classList.add('bg-[#e8f0e0]','text-gray-700','font-medium');
            });
            button.classList.add('bg-[#c1d9a3]','text-[#4a7031]','font-semibold');
            button.classList.remove('bg-[#e8f0e0]','text-gray-700','font-medium');
            langContents.forEach(c => c.classList.toggle('hidden', c.id !== `content-${lang}`));
          });
        });
      }

      // Share: force id-only canonical URL
      const shareBtn = document.getElementById('shareBtn');
      const toast = document.getElementById('toast');
      if (shareBtn && toast) {
        shareBtn.addEventListener('click', async () => {
          const forceUrl = "<%= canonicalUrl %>"; // https://demo.minaramasjid.com/question/{id}
          const title = document.title;
          const descMeta = document.querySelector('meta[name="description"]');
          const text = descMeta ? descMeta.content : '';
          try {
            if (navigator.share) {
              await navigator.share({ title, text, url: forceUrl });
            } else {
              const ta = document.createElement('textarea');
              ta.value = forceUrl; ta.style.position='fixed'; ta.style.left='-9999px';
              document.body.appendChild(ta); ta.focus(); ta.select();
              let ok=false; try { ok = document.execCommand('copy'); } catch(e){}
              document.body.removeChild(ta);
              if (ok) { toast.classList.remove('opacity-0'); setTimeout(()=>toast.classList.add('opacity-0'), 2000); }
            }
          } catch(e){ console.error('Share failed:', e); }
        });
      }
    });
  </script>
</body>
</html>
